{
  "name": "local-agent-gateway-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local MCP gateway for invoking Kimi Code as an external coding agent.",
  "bin": {
    "agent-gateway-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test dist/**/*.test.js",
    "smoke": "npm run build && node dist/tests/mcp-smoke.js",
    "smoke:live": "npm run build && node dist/tests/mcp-smoke.js --live",
    "inspect": "npx @modelcontextprotocol/inspector node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "zod": "3.25.76"
  },
  "devDependencies": {
    "@types/node": "22.15.3",
    "typescript": "5.8.3"
  },
  "engines": {
    "node": ">=20"
  }
}
